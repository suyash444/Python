# using maths function in python

import math as m
# -- whereas m is called alias
print(m.sqrt(24))
print(m.pow(12,2))

# now if we want to use only two maths functions then  we can use this import function

from math import sqrt, pow

print(pow(10,2))
print(sqrt(25))